services:
  redis:
    image: redis:7-alpine
    container_name: nestjs-event-driven
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
        - redis-data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD","redos-cli","ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  redis-data:
